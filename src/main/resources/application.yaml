spring:
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
    properties:
      hibernate:
        format_sql: true
  datasource:
    driver-class-name: org.postgresql.Driver
    username: postgres
    password: qwer
    url: jdbc:postgresql://localhost:5432/db_ecommerce_a01m1
  application:
      name: a01-m1-SpringMVC

